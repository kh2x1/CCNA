/* networkplus.js — CompTIA Network+ track. Content in progress.
   Registers an empty track so the app loads cleanly; lessons are being authored. */
(function () {
  const D = window.NS_DATA = window.NS_DATA || {};
  D.networkplus = D.networkplus || { id: 'networkplus', modules: [] };
})();
