/* quizbank.js — Standalone quiz banks. In progress (lesson quizzes already power the Quiz Center). */
(function () {
  const D = window.NS_DATA = window.NS_DATA || {};
  D.quizBank = D.quizBank || [];
})();
